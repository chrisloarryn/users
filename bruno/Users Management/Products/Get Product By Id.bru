meta {
  name: "Get Product By Id"
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/products/{{productId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt}}
}

settings {
  encodeUrl: true
}
