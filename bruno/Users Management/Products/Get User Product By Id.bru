meta {
  name: "Get User Product By Id"
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/api/users/{{userId}}/products/{{productId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt}}
}

settings {
  encodeUrl: true
}
