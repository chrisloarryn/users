meta {
  name: "List User Products"
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/users/{{userId}}/products
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt}}
}

settings {
  encodeUrl: true
}
